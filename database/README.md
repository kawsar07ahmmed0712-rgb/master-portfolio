# Database Utilities

This folder contains scripts for database provisioning (create DB, seeds, etc).

## Create database
- Uses credentials from `backend/.env`
- Creates DB if missing

Run:
1) Activate backend venv
2) python database/create_database.py
