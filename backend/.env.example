ENVIRONMENT=dev
CORS_ORIGINS=http://localhost:5173

# MySQL (preferred: keep credentials separate)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=07120712
DB_NAME=master_portfolio

# Optional: If you set DATABASE_URL, it will override DB_* above
# DATABASE_URL=mysql+pymysql://root:07120712@localhost:3306/master_portfolio?charset=utf8mb4

SECRET_KEY=change_me_now
ACCESS_TOKEN_EXPIRE_MINUTES=60
